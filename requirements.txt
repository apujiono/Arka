fastapi>=0.104.0
uvicorn[standard]>=0.23.0
pydantic>=2.0
requests
cryptography
python-dotenv

# Optional: kalau pakai ChromaDB
chromadb>=0.4.22
langchain>=0.1.16

# Untuk PDF (opsional)
PyPDF2
pymupdf

# Untuk web scraping (opsional)
feedparser
beautifulsoup4

# Penting: tambah ini agar Railway bisa build
wheel
setuptools
